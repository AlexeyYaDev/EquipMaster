<Window x:Class="EquipMaster.Views.Assignment.ReturnEquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EquipMaster.ViewModels.Assignment"
        mc:Ignorable="d"
        Title="Возврат оборудования"
        Height="300" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.DataContext>
        <vm:ReturnEquipmentViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2"
                   Text="Возврат оборудования"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Выдача:"
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <ComboBox Grid.Row="1" Grid.Column="1"
                  ItemsSource="{Binding ActiveAssignments}"
                  SelectedItem="{Binding SelectedAssignment}"
                  DisplayMemberPath="DisplayInfo"
                  Margin="0,0,0,10"
                  Height="30"/>

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Заметка:"
                   VerticalAlignment="Top"
                   Margin="0,5,10,0"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding ReturnNotes}"
                 Height="60"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Принять"
                    Width="100"
                    Margin="0,0,10,0"
                    Command="{Binding ReturnCommand}"/>
            <Button Content="Отмена"
                    Width="100"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
